class User {
    constructor(id, name, username, active_status) {
        this.id = id;
        this.name = name;
        this.username = username;
        this.active_status = active_status;
    }
}

export default User;